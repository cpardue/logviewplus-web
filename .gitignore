# Logs (runtime only — test fixtures under tests/fixtures/logs are kept)
/logs
*.log
!tests/fixtures/logs/*.log
npm-debug.log*

node_modules
dist
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.sw?

# Project artifacts
tests/fixtures/logs/generated/
test-results/
_*.bak
_play_*

# Temp API/CI probes (must NEVER be committed — the API pusher stages every
# untracked file that is not gitignored)
_probe_*.mjs
_probe_out.txt
